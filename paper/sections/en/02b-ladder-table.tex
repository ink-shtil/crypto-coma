\section{The whole ladder}

Below is the full ladder \(a \dots z\) and its summit. The ``Notation'' column uses the
self-power operator \(\star\) (one star per letter), and the last column tries ---
hopelessly, of course --- to convey \emph{how} much this is. The numbers come from the same
computation as the whole project (\texttt{docs/data/levels.json}).

\input{generated/levels-table-en.tex}
